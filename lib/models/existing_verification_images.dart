class ExistingVerificationImages{
  final String? profileID, faceImage, licenceFrontImage, licenceBackImage;
  ExistingVerificationImages({this.profileID, this.faceImage, this.licenceFrontImage, this.licenceBackImage});
}